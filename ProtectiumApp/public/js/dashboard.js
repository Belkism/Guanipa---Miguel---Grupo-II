window.volverAInicio = () => {
    sessionStorage.clear();
    window.location.href = "/";
}

document.addEventListener("click", (e) => {
   // Editar -> redirige desde html
   // Eliminar -> Falta logica
    
});